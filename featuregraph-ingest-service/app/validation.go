package app

func isAccIdValid(appId string) bool {
	// TODO: here we could validate the format
	return true
}

func isAppIdValid(appId string) bool {
	// TODO: here we could validate the format
	return true
}
